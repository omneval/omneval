package handler

import "github.com/omneval/omneval/services/query/internal/routes"

// DBHandle is the minimal database interface used by query handlers.
// Re-exported from the shared routes package.
type DBHandle = routes.DBHandle
