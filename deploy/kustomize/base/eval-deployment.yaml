apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval
  labels:
    app.kubernetes.io/name: lantern
    app.kubernetes.io/component: eval
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: lantern
      app.kubernetes.io/component: eval
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lantern
        app.kubernetes.io/component: eval
    spec:
      containers:
        - name: eval
          image: lantern/app:latest
          imagePullPolicy: IfNotPresent
          command: ["lantern-eval"]
          env:
            - name: LANTERN_CONFIG
              value: "/etc/lantern/lantern.yaml"
            - name: LANTERN_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: lantern-secret
                  key: redis-password
                  optional: true
            - name: LANTERN_STORAGE_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: lantern-secret
                  key: storage-access-key
                  optional: true
            - name: LANTERN_STORAGE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: lantern-secret
                  key: storage-secret-key
                  optional: true
            - name: LANTERN_EVAL_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: lantern-secret
                  key: eval-llm-api-key
                  optional: true
            - name: LANTERN_DATABASE_DSN
              valueFrom:
                secretKeyRef:
                  name: lantern-secret
                  key: postgres-dsn
                  optional: true
          volumeMounts:
            - name: config
              mountPath: /etc/lantern
              readOnly: true
          livenessProbe:
            exec:
              command:
                - cat
                - /proc/1/status
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: config
          configMap:
            name: lantern-config
