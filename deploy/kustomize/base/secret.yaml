apiVersion: v1
kind: Secret
metadata:
  name: omneval-secret
  labels:
    app.kubernetes.io/name: omneval
type: Opaque
stringData:
  # S3 credentials — set when using S3-compatible object storage
  storage-access-key: ""
  storage-secret-key: ""
  # Judge LLM API key (OpenAI-compatible)
  eval-llm-api-key: ""
  # Redis password (only needed if Redis auth is enabled)
  redis-password: ""
  # Admin bootstrap credentials (only needed for first-time setup)
  admin-password: ""
  admin-email: ""
  # PostgreSQL DSN (only needed when using Postgres metadata store)
  postgres-dsn: ""
  # PostgreSQL password (for internal PostgreSQL instance)
  postgres-password: "omneval"
  # MinIO root credentials (for internal MinIO instance)
  minio-root-user: minio
  minio-root-password: minio123
